{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "measurements": {
          "$measurementId": {
            ".read": "auth != null"
          }
        }
      }
    }
  }
}
